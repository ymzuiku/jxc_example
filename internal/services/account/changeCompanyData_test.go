package account

import (
	"testing"
)

// TODO 修改企业信息

func TestChangeComapnyDataVerify(t *testing.T) {
	t.Run("the boss change account people, company, but not is boss, need return error", func(t *testing.T) {
		// account := mockRegisterEmployee(t)
	})

	t.Run("the boss change account company name, but company name is haved, need return error", func(t *testing.T) {

	})
}

func TestChangeComapnyData(t *testing.T) {
	t.Run("the boss change account people, company", func(t *testing.T) {
		// account := mockRegisterCompany(t)
		// err := ChangeComapnyData(account.ID)
		// so.Nil(t, err)
	})
	t.Run("the boss change account people, company", func(t *testing.T) {

	})
}
