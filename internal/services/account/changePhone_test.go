package account

import (
	"testing"
)

// TODO 修改手机号
func TestChangePhone(t *testing.T) {
	t.Run("Use session and change new phone and code", func(t *testing.T) {})
}
func TestChangePhoneVerify(t *testing.T) {
	t.Run("Use session and change new phone and code, but the phone is other people used, need return error", func(t *testing.T) {})
	t.Run("Use session and change new phone and code, but the phone code is error, need return error", func(t *testing.T) {})
}
